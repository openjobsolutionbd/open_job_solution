# CHANGELOG

এই ফাইলে **সিস্টেম/কাঠামোর** পরিবর্তন লেখা থাকে — নতুন ফিচার, ডিজাইন বদল, ফাইল-কাঠামো বদল ইত্যাদি।

**এখানে থাকে না:** মাসিক কারেন্ট অ্যাফেয়ার্স কনটেন্ট আপডেট (যেমন নতুন GDP সংখ্যা যুক্ত হওয়া) — সেগুলোর ইতিহাস প্রতিটা টপিক ফাইলের "পরিবর্তনের ইতিহাস" অংশে থাকে।

**নিয়ম:** কোনো মেজর পরিবর্তনের আগে Claude ব্যবহারকারীর অনুমতি নেবে। অনুমতি পেলেই ভার্সন বাড়বে এবং এখানে এন্ট্রি যুক্ত হবে।

---

## [1.13.0] — ২০২৬-০৯-২১

**যা করা হয়েছে: `main`-এ আসল branch protection চালু + `safe_merge.sh` + real end-to-end যাচাই**

ব্যবহারকারীর নির্দেশে ১.১১.০/১.১২.০-এর পরও বাকি থাকা তিনটা জিনিস: `main`-এর protection শুধু লিখিত ছিল, merge-এর সময়ের ফাঁক, আর দুই-সেশনের pipeline বাস্তবে (লোকাল সিমুলেশনের বাইরে) কখনো পরীক্ষা হয়নি। বিস্তারিত: BUGFIX.md BUG-30।

**যা করা হয়েছে:**
- `main`-এ classic branch protection সত্যিই চালু: `required_pull_request_reviews` (approving count 0), `enforce_admins: false`, `allow_force_pushes: false`।
- `.github/workflows/update-wiki.yml`: push এখন `WORKFLOW_PAT` (repo-admin-এর PAT) দিয়ে, checkout-এ `persist-credentials: false` — admin-bypass কাজে লাগিয়ে protection সত্ত্বেও bot-এর সরাসরি push অক্ষত রাখা।
- `scripts/safe_merge.sh` (নতুন): `premerge_check.sh`-এর ফল আসার সাথে সাথে সেই sha merge করে — হাতে অপেক্ষার ফাঁক কমায় (পুরোপুরি বন্ধ করে না, তাই ব্যর্থতা-Issue এখনও দরকার)।
- `verify_integration_bugs.py`-তে guard ১০ (WORKFLOW_PAT + persist-credentials, negative-test করা)।
- `PROJECT.md`, `AGENTS.md`, `PR_GUIDE.md` হালনাগাদ — bypass-এর সীমাবদ্ধতা স্পষ্টভাবে লেখা, যাতে ভবিষ্যতের সেশন ভুল ধারণা না করে (আগেরটাই এই বাগের কারণ ছিল)।

**বাস্তবে যাচাই করা (শুধু কোড পড়ে নয়):**
- admin-bypass মেকানিজম আসল repo-তে isolate করে পরীক্ষা: `enforce_admins: true` → push প্রত্যাখ্যাত; `false` → push সফল ("Bypassed rule violations")। test-commit সাথে সাথে revert করে `main` আগের অবস্থায় ফেরানো হয়েছে।
- দুই real branch/PR (archive/ ফাইলে ইচ্ছাকৃত ওভারল্যাপ + একটাতে অবৈধ MCQ-নাম) খুলে CI-তে সংঘর্ষ-সতর্কতা ও নাম-যাচাই দুটোই বাস্তবে ধরা পড়তে দেখা হয়েছে (আগে শুধু pure-function ইউনিট-টেস্টে যাচাই ছিল) — তারপর merge না করে দুটোই বন্ধ ও মুছে ফেলা হয়েছে, সাইটে কোনো প্রভাব পড়েনি।
- এই PR নিজেই merge হওয়ার পর `update-wiki` run-এ `WORKFLOW_PAT`-ভিত্তিক push প্রথমবার আসল Actions-এ প্রমাণিত হবে (VERSION বাম্পের কারণে `changed=true` হয়ে push ধাপ বাস্তবে চলবে) — ফল এই এন্ট্রির ঠিক নিচে confirm করা না থাকলে merge-লগে দেখুন।

**সীমাবদ্ধতা:** bypass identity-ভিত্তিক — admin-অ্যাকাউন্ট `openjobsolutionbd`-এর যেকোনো টোকেন সরাসরি push করেও protection এড়াতে পারে (এই কাজে ব্যবহৃত সেশন-টোকেনসহ)। `safe_merge.sh` ফাঁক কমায়, শূন্য করে না। MCQ ও আর্কাইভ এখনও `consolidate_month.py`-র বাইরে।

---

## [1.12.0] — ২০২৬-০৯-২১

**যা করা হয়েছে: একাধিক-সেশন ব্যবস্থার দুর্বলতা ঠিক (খ: প্রক্রিয়া-নিরাপত্তা) — নীরব-stale, আসল তালা, পরীক্ষা-চেকের পরিধি**

১.১১.০-এর পর নিজের কাজ ভেঙে পাওয়া বাকি দুর্বলতা (প্রতিটা বাস্তবে চালিয়ে প্রমাণ করা); বিস্তারিত `BUGFIX.md` BUG-27/28/29।

- **নীরব-stale ঠেকানো (সবচেয়ে গুরুতর):** `update-wiki.yml` ব্যর্থ হলে `site-build-failed` লেবেলে GitHub Issue নিজে খোলে/হালনাগাদ হয় (লগের শেষ লাইনসহ), সফল run-এ নিজে বন্ধ হয় (`issues: write`); build-সতর্কতা Actions-সারাংশে; `workflow_dispatch` + `simulate_failure` (সাইট না ছুঁয়ে সতর্কতা-পথ পরীক্ষা / জরুরি হাতে-rebuild)।
- `scripts/premerge_check.sh` (নতুন): merge-এর ঠিক আগে branch + এই মুহূর্তের `main` মিলিয়ে অস্থায়ী worktree-তে build/verify/টেস্ট — "দুটো PR একা ঠিক, একসাথে ভাঙা" ধরে। `PR_GUIDE.md`-এ auto-merge শর্ত ও ধাপ ৬-এ যুক্ত।
- `scripts/site_status.py` (নতুন) + `session_status.sh`: প্রতি সেশনের শুরুতে সর্বশেষ `update-wiki` run ও খোলা Issue (🚨)।
- `scripts/claim_check.sh`: `--claim` = পারমাণবিক তালা (GitHub-এ `claim/<নাম>` ref; দুই সেশন একসাথে চাইলে ঠিক একজন পায়), `--release`, শব্দ-সীমা মেলানো (সাবস্ট্রিং-এর মিথ্যা-দখল বন্ধ), ৩+ দিনের পুরনো দখল চিহ্নিত।
- `scripts/pr_checks.py`: `archive/` যুক্ত; শেয়ার্ড নথিতে ওভারল্যাপ ℹ️ তথ্য-নোট (ব্যর্থ করে না); নতুন ফাইলের নাম যাচাই (`<YYYY-MM>-<স্কোপ>.md`; পুরনো রেঞ্জ-নাম ও অঙ্ক-শুরু স্কোপ ব্যর্থ)। এখন env ছাড়াই import হয়।
- `scripts/pr_build_warnings.py` (নতুন) + `pr-check.yml`: build-সতর্কতা PR-কমেন্টে (আপডেট হয়, ব্যর্থ করে না)। `preflight.sh` সতর্কতা দেখায় (আগের `PR_GUIDE.md`-এর দাবি ভুল ছিল)।
- `scripts/test_pr_checks.py` (নতুন, ১১টা টেস্ট; `preflight.sh` ও `pr-check.yml`-এ); `verify_integration_bugs.py`-এ guard ৮, ৯ (negative-test করা)।
- `AGENTS.md`, `PR_GUIDE.md` হালনাগাদ (নতুন স্ক্রিপ্ট, ঠিক করা দাবি, ধাপ ৯–১০)।

**সীমা:** (১) `premerge_check.sh` "এই মুহূর্তের" `main`-এর সাথে চেক করে — মাঝখানে main আরও এগোলে ঝুঁকি থাকে; সেই কারণেই ব্যর্থতা-Issue। (২) আসল তালা কাজ করে শুধু যারা `--claim` ব্যবহার করে; না করলে শুধু-দেখার সীমা থাকে। (৩) `VERSION`-এ সিস্টেম-PR (হাতে বাড়ানো ভার্সন) আর bot-এর স্বয়ংক্রিয় বাড়ানো একই ফাইল ছোঁয় — মাঝে কোনো কনটেন্ট merge হলে rebase-এ সংঘর্ষ হয় (এই কাজেই একবার হয়েছিল); সিস্টেম-PR-এ ভার্সন শেষ মুহূর্তে বাড়ান। (৪) `main`-এ protection এখনো নেই।

---

## [1.11.0] — ২০২৬-০৯-২০

**যা করা হয়েছে: একাধিক-সেশন ব্যবস্থার নিজস্ব দুর্বলতা খুঁজে ঠিক করা (ক: ডেটা-নিরাপত্তা)**

১.১০.০ (প্রতি সেশন নিজের ফাইল) লাইভ করার পর নিজের কাজ ভেঙে পরীক্ষা করে বেশ কিছু দুর্বলতা পাওয়া গেছে (প্রতিটা বাস্তবে চালিয়ে প্রমাণ করা)। এই ভাগে ডেটা-সংক্রান্তগুলো:

- `build_index.py` — **ডুপ্লিকেট বাদ দিতে গিয়ে টপিক-লিংক হারানো:** দুই ফাইলে একই বুলেটের একটাতে `[[লিংক]]` থাকলে ফাইল-ক্রমে লিংক-ওয়ালাটাই বাদ পড়ত (নীরবে)। এখন বাদ-পড়াটার টপিক-লিংক রাখা বুলেটে যোগ হয় (ওভারল্যাপ/অনুপস্থিত বাক্যাংশ বাদে)।
- `build_index.py` — **টাইপো-তারিখ মিশে যাওয়া:** দুই ফাইলের ভিন্ন টাইপো-মাস ('আগষ্ট', 'সেপ্টেবর') একই দিনে জোড়া লেগে দ্বিতীয় হেডিং গিলে ফেলত। এখন মাস-চেনা-যায়-না হেডিং হুবহু একই লেখা হলেই কেবল জোড়া লাগে।
- `build_index.py` — **বিভাগের ক্রম:** ফাইল-ক্রমে আন্তর্জাতিক আগে আসতে পারত; এখন জোড়া-লাগা দিনে সবসময় বাংলাদেশ→আন্তর্জাতিক (একক ফাইলের দিনের ক্রম অপরিবর্তিত)। MCQ-তে মূল মাসিক ফাইলের সেকশন আগে, সেশন-ফাইল পরে। অঙ্ক-দিয়ে-শুরু MCQ স্কোপে সতর্কতা (আলাদা সেট হয়ে যাচ্ছিল নীরবে)।
- `build_index.py` — **আলাদা শব্দে লেখা প্রায়-একই ঘটনা** (সমানতা ≥ ৮৫%) দুই ফাইলে থাকলে সতর্কতা (দুটোই থাকে)।
- `scripts/consolidate_month.py` (নতুন, ঐচ্ছিক) — মাসশেষে ঘটনাপ্রবাহ/টপ নিউজের সেশন-ফাইল একত্র; build আগে-পরে ইনডেক্স হুবহু মিলিয়ে দেখে, না মিললে নিজে রোলব্যাক। ভিন্ন লিংক-মার্কআপে ডুপ্লিকেট বা বহু-লাইনের বুলেটে থেমে হাতে মেলাতে বলে।
- `test_build_index.py` — ৯টা নতুন টেস্ট (মোট ২০/২০); ছয়টা আচরণ-টেস্ট পুরনো কোডে ব্যর্থ, নতুনে পাস। বিদ্যমান ডেটায় আউটপুট হুবহু অপরিবর্তিত।
- `AGENTS.md` স্ক্রিপ্ট-তালিকায় `claim_check.sh` (আগে বাদ পড়েছিল) ও `consolidate_month.py`; `PR_GUIDE.md` হালনাগাদ; ২০২৫-১১/১২ ঘটনাপ্রবাহ ফাইলের পুরনো "rename করে extend" নোট সংশোধন।
- **যাচাই করে দেখা, সমস্যা নেই:** টপ নিউজে একই তারিখের একাধিক হাইলাইট ফ্রন্টএন্ড আলাদা আলাদা সারিতে দেখায় (তারিখ প্রতিটায় থাকে) — ভাঙে না। দুই সেশনের একই-তারিখ অবস্থায় `verify_site.py`-ও পাস।

**পরের ভাগ (খ: প্রক্রিয়া-নিরাপত্তা)** আলাদা PR-এ: ব্যর্থ build-এর সতর্কতা, merge-এর আগে "main-এর সাথে মিলিয়ে" চেক, আসল তালা, পরীক্ষা-চেকের পরিধি।

---

## [1.10.0] — ২০২৬-০৯-২০

**যা করা হয়েছে: একাধিক সেশন একসাথে সংঘর্ষ ছাড়া কাজ করার ব্যবস্থা — প্রতি সেশন নিজের নতুন ফাইল**

শর্ত: প্রতিটা সেশন আলাদা টপিক নেয়, একটা টপিক কখনো দুই সেশনে ভাগ হয় না। কিন্তু টপিক-ফাইলের বাইরে কয়েকটা শেয়ার্ড ফাইলে সব সেশন লিখত — `archive/2026-09.md` (হেডার-নোট ও সেকশন-ক্রম), ঘটনাপ্রবাহ/টপ নিউজের মাসিক রেঞ্জ-ফাইল ("rename করে extend" নিয়মে দুই সেশন একই ফাইল ভিন্ন নামে rename করলে rename-সংঘর্ষ, ফাইলের শেষে যোগ করলে কনটেন্ট-সংঘর্ষ)। সংঘর্ষের আসল উৎস এগুলোই।

**যা করা হয়েছে:**
- `scripts/build_index.py`: ঘটনাপ্রবাহ ও টপ নিউজে একই তারিখ একাধিক ফাইলে থাকলে আর বিল্ড-এরর নয় — একটা দিনে জোড়া লাগে ('০১ আগস্ট' ও '১ আগস্ট' একই দিন)। একই ফাইলের ভেতরে ডুপ্লিকেট তারিখ-হেডিং এখনও এরর। দুই ফাইলে হুবহু একই বুলেট থাকলে বিল্ড থামে না (নইলে দুই আলাদা PR আলাদাভাবে পাস করে merge হওয়ার পর বিল্ড ভাঙত, bot generated ফাইল আপডেট করতে পারত না, সাইট stale থাকত) — একটা রেখে stderr-এ সতর্কতা। MCQ-তে `<YYYY-MM>-<স্কোপ>.md` সেশন-ফাইলগুলো একই মাসের একটা সেটে জোড়া লাগে। বিদ্যমান ডেটায় আউটপুট হুবহু অপরিবর্তিত (যাচাই করা)।
- `scripts/test_build_index.py`: ৬টা নতুন regression টেস্ট (পুরনো কোডে ব্যর্থ, নতুনে পাস — যাচাই করা)।
- **নিয়ম বদল:** ঘটনাপ্রবাহ/টপ নিউজ/MCQ/আর্কাইভে এখন প্রতি সেশন নিজের নতুন ফাইল বানায় (`<YYYY-MM>-<স্কোপ>.md`); ২০২৬-০৮-০৮-এর "সংলগ্ন হলে `git mv` দিয়ে rename করে extend" নিয়ম বাতিল। বিদ্যমান মাসিক ফাইলগুলো আর সম্পাদনা হয় না। `AGENTS.md`, `MCQ_GUIDE.md`, `EDITORIAL_MEMORY.md`, `PROJECT.md`, `PR_GUIDE.md` (নতুন "একাধিক সেশন একসাথে কাজ" অংশ) হালনাগাদ।
- `scripts/claim_check.sh`: কাজ শুরুর আগে দেখায় অন্য কোনো খোলা PR/branch ওই টপিক বা ফাইল ছুঁয়ে আছে কিনা (`session_status.sh` থেকেও উল্লেখ)।
- `.gitattributes`: শুধু-যোগ-হওয়া `CHANGELOG.md`-এ `merge=union` (লোকাল merge-এ সংঘর্ষ-চিহ্ন বসে না)।

**সীমাবদ্ধতা:** (১) GitHub-এর Merge বোতাম `.gitattributes` মানে না — সংঘর্ষ হলে সেশনকে লোকালে `git merge origin/main` করতে হয়। (২) একই বিদ্যমান টপিক-ফাইল দুই সেশন একসাথে বদলালে সংঘর্ষ আটকানো যায় না — `claim_check.sh` আগে থেকে জানায়, আর `pr_checks.py` অন্য খোলা PR-এর সাথে একই ফাইল বদলালে ব্যর্থ হয়। (৩) `main`-এ protection না থাকায় "branch+PR" নিয়ম এখনো শুধু লিখিত।

---

## [1.9.0] — ২০২৬-০৯-২০

**যা করা হয়েছে: `update-wiki.yml` আর rebuild-PR খোলে না — generated output সরাসরি `main`-এ commit করে**

কনটেন্ট merge হওয়ার পর লাইভ সাইটে নতুন তথ্য না পৌঁছানোর আসল কারণ ছিল generated ফাইলের (`topics-index.json`, `sw.js`, `version.json`, `docs/topic/` …) হালনাগাদ একটা bot-PR-এ (`auto/rebuild-output`) আটকে থাকা — সেটা হাতে merge না হওয়া পর্যন্ত সাইট stale থাকত (PR #137: ~২০ দিন)। PR-এর `pr-check` `action_required`-এ আটকাত কারণ bot-এর প্রতিটা push হতো `github-actions[bot]` নামে (`actions/checkout`-এর সংরক্ষিত `GITHUB_TOKEN` push-URL-এর PAT-কে ছাপিয়ে যেত) — `BOT_PAT`/`WORKFLOW_PAT` কখনো `git push`-এ ব্যবহারই হয়নি, শুধু API-কলে। বিস্তারিত: `BUGFIX.md` BUG-26।

**যা করা হয়েছে:**
- `.github/workflows/update-wiki.yml`: PR তৈরি/আপডেটের ধাপ বাদ; নতুন ধাপ generated output `GITHUB_TOKEN` দিয়ে সরাসরি `main`-এ commit+push করে। সমান্তরাল merge-এ main এগিয়ে গেলে নিরাপদে push বাদ দেয় (নতুন commit-এর run বানায়); অন্য কারণে ব্যর্থ হলে স্পষ্ট `::error::`। `permissions` থেকে `pull-requests: write` সরানো হয়েছে।
- `scripts/verify_integration_bugs.py`: দুটো নতুন regression guard — (৬) push-URL-এ PAT থাকলে checkout-এ `persist-credentials: false` বাধ্যতামূলক, (৭) workflow-কোডে skip-ci ট্যাগ নিষিদ্ধ।
- `AGENTS.md`, `PR_GUIDE.md`, `PROJECT.md`: "rebuild PR-ও merge করতে হবে" নির্দেশ সরিয়ে নতুন প্রবাহ লেখা; `scripts/session_status.sh`-এর ভুল কারণ-ব্যাখ্যা সংশোধন; `pr_checks.py`/`safe_add.sh`-এর মন্তব্য হালনাগাদ।
- `BOT_PAT`/`WORKFLOW_PAT` secret এই workflow-এ আর ব্যবহৃত হয় না।

**মনে রাখার বিষয়:** সরাসরি push কাজ করে কেবল `main`-এ branch protection/ruleset না থাকলে (২০২৬-০৯-২০-এ GitHub API-তে কোনোটাই ছিল না)। protection চালু করলে আগে github-actions-কে bypass দিন, নইলে workflow ব্যর্থ হবে ও সাইট আবার stale থাকবে। কনটেন্ট/কোড বদলের branch+PR-নিয়ম অপরিবর্তিত।

## [1.8.0] — ২০২৬-০৮-২৯

**যা করা হয়েছে: ৩-ধাপ `archive-staging/` সিস্টেম সম্পূর্ণ বাদ, এক-ধাপ সরাসরি-প্রসেসিং ওয়ার্কফ্লো চালু**

২০২৬-০৮ পর্যন্ত নতুন ম্যাগাজিন সংখ্যা প্রসেস করতে একটা ৩-ধাপ পদ্ধতি ছিল: (১) স্টেজিং — OCR করা কনটেন্ট `archive-staging/YYYY-MM/`-এ verbatim `.md` ফাইল হিসেবে জমা, প্রতিটা সংখ্যার নিজস্ব `STATUS.md`-এ pending/confirmed ট্র্যাক করা; (২) প্রসেসিং — আলাদা সেশনে স্টেজিং থেকে কনটেন্ট `docs/topics/`, `docs/ghotonaprobaho/`, `docs/mcq/`, `docs/top-news/`-এ ভাগ করে distribute; (৩) ফাইনাল আর্কাইভ — `archive/`-এ মাসিক ফাইল।

ব্যবহারকারীর সিদ্ধান্তে এই সিস্টেম বাদ দেওয়া হয়েছে। কারণ: (ক) একাধিক সমান্তরাল Claude সেশন একই সময়ে একই staging ফাইল/`STATUS.md`-এ হাত দেওয়ার সংঘর্ষ (এই ভার্সনের কাজ চলাকালীনও বাস্তবে ঘটেছে — একটা সেশন `archive-staging/2026-03/` ফোল্ডার মুছে ফেলেছিল ভুল দাবি করে যে কনটেন্ট আগেই `docs/`-এ গেছে, বাস্তবে যায়নি; git history থেকে recover করে প্রসেস করতে হয়েছে); (খ) দুই আলাদা সেশনে কাজ ভাগ হওয়ার overhead — স্টেজিং সেশন শেষ হওয়ার পর আবার নতুন সেশনে "প্রসেস করো" বলে মনে করিয়ে দেওয়া লাগত।

**নতুন ওয়ার্কফ্লো (এক-ধাপ):** ম্যাগাজিন পেজ → OCR/জুম করে পড়া → অনিশ্চিত অংশ ব্যবহারকারীকে জিজ্ঞেস/ওয়েব সার্চে যাচাই → `check_topic.sh` দিয়ে ডুপ্লিকেট-চেক → টপিক-বাই-টপিক কনফার্মেশন → **সরাসরি** `docs/topics/`, `docs/ghotonaprobaho/`, `docs/top-news/`, বা `docs/mcq/`-এ বসানো (কোনো intermediate staging ফাইল ছাড়াই) → `build_index.py`/`verify_site.py` validate → commit। বিস্তারিত: `AGENTS.md`-এর নতুন "নতুন ম্যাগাজিন সংখ্যা/স্ক্যান থেকে কনটেন্ট যোগ" সেকশন ও `PROJECT.md`-এর ৭নং সেকশন।

**যা করা হয়েছে:**
- `archive-staging/` ফোল্ডার (২০২৬-০১, ২০২৬-০৮, standalone/২০২৬-০৮ — সব প্রসেসড বলে যাচাই করে) ও `STAGING_GUIDE.md` মুছে ফেলা হয়েছে।
- `AGENTS.md`, `PROJECT.md`, `README.md` থেকে ৩-ধাপ staging-এর বর্ণনা সরিয়ে নতুন এক-ধাপ ওয়ার্কফ্লো ডকুমেন্ট করা হয়েছে।
- `scripts/pr_checks.py`-এর `SOURCE_PREFIXES`-এ থাকা `archive-staging/` এন্ট্রি সরানো হয়েছে (আর প্রাসঙ্গিক না)।
- মুছে ফেলার আগে `archive-staging/2026-03/`-এর সব confirmed/pending পেজ (git history থেকে recover করে) `docs/`-এ প্রসেস করা হয়েছে — নতুন টপিক, ঘটনাপ্রবাহ/টপ-নিউজ এন্ট্রি, MCQ আর্কাইভ, এবং বিদ্যমান টপিকে (`report-somikkha.md`, `sommelon-boithok.md`, `podok-purushkar.md`) merge।
- `archive/` (ফাইনাল মাসিক আর্কাইভ) ফোল্ডার অপরিবর্তিত রাখা হয়েছে — এটা আলাদা, ঐচ্ছিক concept, staging বাদ দেওয়ার সাথে সরাসরি সম্পর্কিত না।

**সংশোধন এই কাজের সময়:** প্রথমে ভুলবশত `EDITORIAL_MEMORY.md`-এর "অনন্তলোকে ডিফল্ট বাদ" নিয়ম না মেনে একটা পূর্ণ ১২-জনের অনন্তলোকে টপিক তৈরি হয়েছিল এবং পদক-পুরস্কার টপিকের জন্য নতুন ডুপ্লিকেট ফাইল বানানো হয়েছিল বিদ্যমান `podok-purushkar.md`-এ merge না করে — সমান্তরাল সেশনের rebase-এ ধরা পড়ে দুটোই সংশোধন করা হয়েছে।

---

## [1.7.0] — ২০২৬-০৮-১১

**যা করা হয়েছে: সরাসরি `main`-push বাদ দিয়ে branch+PR ওয়ার্কফ্লো চালু, স্বয়ংক্রিয় PR-চেক**

ব্যবহারকারী একই সাথে একাধিক Claude account/চ্যাট থেকে এই রিপোতে কাজ করান — একাধিক সেশনের সমান্তরাল সরাসরি-push একে অপরের কাজ না দেখেই সংঘর্ষ/ওভাররাইট করার ঝুঁকি তৈরি করছিল (এই ভার্সন তৈরির প্রক্রিয়াতেই তার বাস্তব উদাহরণ ঘটেছে — নিচের [Unreleased] এন্ট্রিটা ঠিক এই মুহূর্তে আরেকটা সেশন থেকে সরাসরি `main`-এ পুশ হয়েছিল)। এখন থেকে প্রতিটা সেশন নিজের branch-এ কাজ শেষ করে একটা Pull Request (PR) খোলে; `main`-এ merge (লাইভ) হওয়ার আগ পর্যন্ত PR-টাই "খসড়া/অপেক্ষার ঘর"। ব্যবহারকারীর স্পষ্ট অনুমোদন ছাড়া কোনো merge হয় না — কোনো auto-merge নেই, এমনকি সব চেক পাস করলেও না।

**১. `.github/workflows/pr-check.yml`**
PR খোলা/আপডেট হলে চালায়: (ক) auto-generated ফাইল (`docs/topics-index.json`, `docs/topic/`, `docs/sitemap.xml` ইত্যাদি) হাতে বদলানো হয়েছে কিনা — এগুলো শুধু main-এ merge হওয়ার পর `build_index.py` নিজে তৈরি করার কথা; (খ) অন্য কোনো *খোলা* PR-এর সাথে ফাইল-ওভারল্যাপ আছে কিনা — দুইটা সেশন একই ফাইলে সমান্তরালে হাত দিলে সেটা real git conflict না হয়েও বিষয়বস্তু-সংঘর্ষ হতে পারে, তাই এটা আলাদাভাবে ধরা হয়; (গ) বিদ্যমান `build_index.py`/`verify_site.py` স্ট্রাকচার-ভ্যালিডেশন।

**২. `scripts/pr_checks.py`**
উপরের (ক)/(খ) চেক করে সমস্যা পেলে PR-এ কমেন্ট পোস্ট করে (GitHub API, `urllib` দিয়ে — বাড়তি dependency ছাড়া)।

**৩. `AGENTS.md` আপডেট**
নতুন "PR-ভিত্তিক ওয়ার্কফ্লো" সেকশন যোগ হয়েছে — branch তৈরি, push, PR খোলা/লিস্ট করা/মার্জ করা/branch মোছার সব কমান্ড-রেফারেন্স। পুরনো rebase/force-push নির্দেশনা সরিয়ে branch+PR-ভিত্তিক conflict-হ্যান্ডলিং দিয়ে প্রতিস্থাপন করা হয়েছে। টোকেন-স্কোপের নির্দেশনা আপডেট: এখন থেকে "Contents" ছাড়াও "Pull requests" স্কোপ লাগবে।

**সিদ্ধান্ত — AI-সারাংশ যোগ করা হয়নি:** প্রথমে পরিকল্পনা ছিল প্রতিটা PR-এ Claude API দিয়ে একটা স্বয়ংক্রিয় সহজ-বাংলা সারাংশ কমেন্ট যোগ করা, কিন্তু Anthropic API-তে স্থায়ী ফ্রি লেভেল নেই বলে ব্যবহারকারী এটা বাদ দিতে বলেছেন। এর পরিবর্তে: PR-এর পরিবর্তন যেকোনো সেশনে জিজ্ঞেস করা হলে Claude নিজে diff পড়ে সরাসরি চ্যাটে সহজ বাংলায় ব্যাখ্যা করবে — কোনো বাড়তি API/খরচ ছাড়াই।

**সীমাবদ্ধতা/ভবিষ্যতে করণীয়:** `main` branch protection (GitHub Settings-এ PR বাধ্যতামূলক করা) এখনো সেট করা হয়নি — সেটার জন্য Administration স্কোপসহ একটা এককালীন সেটআপ টোকেন লাগবে।

---

## [Unreleased] — automated bug-regression suite যোগ + BUG-01 ফিক্স পুনরুদ্ধার, ২০২৬-০৮-১০

**যা করা হয়েছে: ভবিষ্যতে feature/code পরিবর্তনে পুরনো bug (BUGFIX.md) যেন চুপচাপ আবার ফিরে না আসে, তার জন্য একটা স্থায়ী automated regression-test system (ব্যবহারকারীর অনুরোধে)**

**১. `scripts/js_tests/`** — jsdom-ভিত্তিক suite। `docs/index.html`-এর app-shell script থেকে bootstrap chain বাদ দিয়ে (definitions-only) সরাসরি আসল প্রোডাকশন কোড extract করে চালায় — কোনো পুনর্লিখিত/সরলীকৃত কপি নয়। BUG-03, 04, 05, 06, 07, 08, 09 — প্রতিটা bug-ক্লাসের জন্য একটা করে টেস্ট (মোট ৮টা)।

**২. `scripts/test_build_index.py`** — BUG-10-এর জন্য pure-Python regression test (কোনো npm dependency লাগে না)।

**৩. `scripts/verify_site.py`-তে দুটো নতুন static check** — (৮) `@font-face`-এর প্রতিটা local font-url `sw_template.js`-এর `APP_SHELL`-এ আছে কিনা (BUG-02 ক্লাস), (৯) `@media print`-এ `.card`-এর `display:block !important` আছে কিনা (BUG-01)।

**৪. `scripts/preflight.sh`** এখন push-এর আগে এগুলো স্বয়ংক্রিয়ভাবে চালায় — Python টেস্টগুলো সবসময় (সস্তা), কিন্তু `js_tests/` শুধু তখনই যখন `docs/index.html`/`scripts/sw_template.js`-এর মতো code ফাইল বদলায় (`git diff --name-only origin/main` দিয়ে চেক করে) — content-only টপিক আপডেটে টোকেন/সময় খরচ বাড়ে না।

**৫. `BUG-01` ফিক্স পুনরুদ্ধার** — নতুন check-৯ লেখার সময় ধরা পড়ে যে BUGFIX.md-এ "ফিক্সড" লেখা থাকা সত্ত্বেও `docs/index.html`-এর লাইভ কোডে `@media print`-এর `.card` rule-এ `display:block !important` আসলে ছিল না (কীভাবে হারিয়েছিল অজানা)। অর্থাৎ মোবাইলে article না খুলে প্রিন্ট/PDF করলে bug-টা এখনো লাইভ ছিল। পুনরায় বসানো হলো। বিস্তারিত: `BUGFIX.md` BUG-01 এন্ট্রির ২০২৬-০৮-১০ নোট ও "ব্যাচ ২" সেকশন।

**৬. `AGENTS.md`** — ফাইল-কাঠামো টেবিলে নতুন ফাইলগুলো (ও আগে থেকে-থাকা কিন্তু টেবিলে না-থাকা `BUGFIX.md`/`TEST_CHECKLIST.md`) যোগ, এবং একটা নতুন নিয়ম: ভবিষ্যতে কোনো bug ঠিক করলে শুধু `BUGFIX.md`-এ লিখলেই চলবে না, একটা matching automated test-ও যোগ করতে হবে।

**যাচাই পদ্ধতি:** প্রতিটা নতুন check নিজে থেকেই স্বনির্ভরভাবে যাচাই করা হয়েছে — bug ইচ্ছাকৃতভাবে সাময়িকভাবে পুনরায় ঢুকিয়ে (production ফাইলে সরাসরি, কমিট না করে) নিশ্চিত করা হয়েছে যে সংশ্লিষ্ট নতুন test/check আসলেই fail করে, তারপর আসল ফিক্স পুনরায় বসিয়ে সব পাস নিশ্চিত করা হয়েছে (`renderTopicContent` race guard, `.card` print CSS, ও একটা font APP_SHELL entry — এই তিনটার প্রতিটার জন্য আলাদাভাবে)। পুরো `bash scripts/preflight.sh` শেষে ক্লিন পাস করেছে।

**সংশোধন করা হয়নি:** BUGFIX.md-এর বাকি ৯টা bug-এর ফিক্স সরাসরি টেস্ট/সোর্স-যাচাইয়ে লাইভ কোডে সঠিকভাবে আছে বলে নিশ্চিত হয়েছে — শুধু BUG-01-এর ফিক্স-ই অনুপস্থিত পাওয়া গেছে।

VERSION বাড়ানো হয়নি — এটা মূলত dev-tooling সংযোজন। BUG-01 ফিক্সটা real হলেও ছোট, non-breaking (কোনো ব্যবহারকারী নতুন করে ভাঙা কিছু পাবে না, শুধু আগের-ভাঙা প্রিন্ট-আউটপুট ঠিক হবে) — আগের একই-ধরনের অডিট-ফিক্স এন্ট্রির (২০২৬-০৭-২৬) মতো একই যুক্তি প্রযোজ্য। ব্যবহারকারী চাইলে বলবেন, বাম্প করে দেওয়া হবে যাতে ইতিমধ্যে-ক্যাশড PWA ব্যবহারকারীরাও দ্রুত নতুন cache পান।

---

## [1.6.0] — ২০২৬-০৮-০২

**যা করা হয়েছে: robots.txt-এ AI-ট্রেনিং রোবট ব্লক, ⭐ প্রিয় (বুকমার্ক) তালিকা, মাসিক সারাংশ প্রিন্ট/PDF, প্রিন্ট ওয়াটারমার্ক (সব ব্যবহারকারীর অনুরোধে)**

**১. robots.txt: গুগল সার্চ চালু রেখে পরিচিত AI-ট্রেনিং রোবট নিষেধ**
sitemap.xml/topic-পাতা (VERSION 1.5.0) যোগ হওয়ার পর ব্যবহারকারীর উদ্বেগ ছিল যে এটা AI মডেল-ট্রেনিং রোবটদেরও কনটেন্ট সংগ্রহ সহজ করে দেয়। `scripts/build_index.py`-এর `generate_sitemap_and_robots()`-এ এখন robots.txt-এ Googlebot স্পষ্টভাবে অনুমোদিত রেখে GPTBot, Google-Extended, ClaudeBot, CCBot, Bytespider, Meta-ExternalAgent, Applebot-Extended — পরিচিত AI-ট্রেনিং রোবট নিষেধ করা হয়েছে। বাকি সব রোবটের জন্য সাধারণ `Allow: /` অপরিবর্তিত (Bing-এর মতো অন্যান্য সার্চ ইঞ্জিন প্রভাবিত হয় না)। সীমাবদ্ধতা: robots.txt স্বেচ্ছা-মেনে-চলার নিয়ম, জোর করে আটকানোর ব্যবস্থা না — Cloudflare-এর অ্যাকাউন্ট-লেভেল "AI Crawl Control" (ড্যাশবোর্ডে, কোডে না) নেটওয়ার্ক-লেভেল বিকল্প হিসেবে ব্যবহারকারীকে জানানো হয়েছে।

**২. ⭐ প্রিয় (বুকমার্ক) তালিকা**
'পড়া হয়েছে' ট্র্যাকিংয়ের সাথে সামঞ্জস্যপূর্ণ প্যাটার্নে: প্রতিটা টপিকে ☆/⭐ 'প্রিয়' বাটন (localStorage key: `oca_bookmarks`), বাম পাশের তালিকায় '⭐ শুধু প্রিয়' ফিল্টার টগল, তালিকায় ⭐ ব্যাজ। নতুন `visibleTopics(q)` র‍্যাপার ফাংশন `currentFiltered(q)`-এর ওপর ঐচ্ছিক বুকমার্ক-ফিল্টার প্রয়োগ করে; ভাসমান সার্চ (যার কাজ যেকোনো টপিক খুঁজে বের করা) ইচ্ছাকৃতভাবে এই ফিল্টারের বাইরে রাখা হয়েছে।

**৩. মাসিক সারাংশ প্রিন্ট/PDF**
'দৈনিক ঘটনাপ্রবাহ' ট্যাবে প্রতিটা মাসের হেডিং-এর পাশে '🖨️ এই মাসের সারাংশ প্রিন্ট/PDF' বাটন — সেই মাসের সব ঘটনাপ্রবাহ + একই মাসে পড়া টপ নিউজ হাইলাইট একসাথে করে বিদ্যমান `window.print()`/`@media print` মেকানিজম পুনর্ব্যবহার করে প্রিন্ট করে (নতুন কোনো PDF লাইব্রেরি লাগেনি)। টপ-নিউজ আইটেমে আলাদা month_key না থাকায় নতুন `parseBengaliDateToMonthKey()` যোগ হলো — `scripts/build_index.py`-এর `BENGALI_MONTHS` ম্যাপিংয়ের JS-সংস্করণ, দুটোর মিল যাচাই করা হয়েছে।

**৪. প্রিন্ট/PDF আউটপুটে ক্লিকযোগ্য ওয়াটারমার্ক**
প্রতিটা প্রিন্ট করা পাতার (টপিক ও মাসিক সারাংশ উভয়) নিচে 'open-current-affairs.pages.dev' লেখা ছোট লিংক — PDF হিসেবে সেভ করলে সেই লিংক ক্লিকযোগ্য থাকে। `position:fixed` + `@media print` ব্যবহার করে প্রতিটা পাতায় পুনরাবৃত্তি হয় (Chrome-এ যাচাই করা প্রমিত পদ্ধতি); `.card`-এ `padding-bottom` যোগ করে কন্টেন্টের সাথে ওভারল্যাপ এড়ানো হয়েছে।

**যাচাই পদ্ধতি:** প্রতিটা ফিচারের জন্য আলাদাভাবে — বুকমার্ক টগল/ফিল্টার লজিক ও Bengali তারিখ-পার্সিং Node-এ মক ডেটা দিয়ে টেস্ট; মাসিক-সারাংশ HTML-জেনারেশন বাস্তব ডেটা দিয়ে সিমুলেট করে ট্যাগ-ব্যালান্স যাচাই (এই প্রক্রিয়ায় তারিখ-রূপান্তর কোডে একটা নিজস্ব ভুল ধরা পড়ে ও ঠিক করা হয়); সব ক্ষেত্রে JS সিনট্যাক্স চেক পাস; robots.txt-এর AI-বট তালিকা সাম্প্রতিক তথ্য যাচাই করে বানানো হয়েছে।

**VERSION বাড়ানো হলো:** 1.5.0 → 1.6.0 (একাধিক নতুন system-level ফিচার, breaking change না)।

---

**যা করা হয়েছে: PROJECT.md, README.md, AGENTS.md, TEST_CHECKLIST.md-এ পুরনো/না-থাকা তথ্য ঠিক করা হলো (ব্যবহারকারীর অনুরোধে)**

**ঠিক করা হয়েছে:**
- `PROJECT.md` — অনেক আগেই সরিয়ে ফেলা "recent changes" (`docs/recent-changes.json`) ফিচারের বর্ণনা ৩ জায়গা থেকে বাদ দেওয়া হলো (ফাইল-ট্রি, প্রোজ, build flow)। file tree ও build flow-তে `docs/top-news/`, `docs/top-news-index.json`, `docs/topic/<slug>/` (SEO পাতা), `docs/sitemap.xml`, `docs/robots.txt` যোগ করা হলো, যাতে `scripts/build_index.py`-এর আসল ধাপগুলোর সাথে মিলে যায়।
- `TEST_CHECKLIST.md` — না-থাকা "recent changes" view-এর চেক-আইটেম বাদ; টপ নিউজ ট্যাব ও SEO পাতা/sitemap/robots.txt-এর জন্য নতুন চেক-আইটেম যোগ।
- `README.md` — `VERSION` রেফারেন্স (`1.4.0` লেখা ছিল, আসল `1.5.0`) ঠিক করা হলো; "এই রিপোতে কী কী আছে" ও "Website-এর ফিচার" অংশে `docs/top-news/`, `docs/topic/<slug>/`, `sitemap.xml`, `robots.txt` যোগ করা হলো, যেগুলো আগে ডকুমেন্টেশনে ছিল না।
- `AGENTS.md` — "বর্তমান অবস্থা"-এ VERSION `1.4.0` থেকে `1.5.0` এবং যাচাইয়ের তারিখ আপডেট করা হলো।

VERSION বাড়ানো হয়নি — খাঁটি ডকুমেন্টেশন সংশোধন, কোনো কোড বা ফিচার বদলায়নি।

---

## [1.5.0] — ২০২৬-০৮-০২

**যা করা হয়েছে: প্রতিটা টপিকের জন্য আলাদা crawlable SEO পাতা + sitemap/robots.txt**

**সমস্যা যা ছিল:** সাইটটা single-page-application — সব টপিক `/#slug` (hash) ফরম্যাটে খোলে। সার্চ ইঞ্জিন সাধারণত hash-এর পরের অংশ আলাদা পাতা হিসেবে ইনডেক্স করে না, তাই গুগলে কোনো নির্দিষ্ট টপিকের নাম খুঁজলে সেই টপিক আলাদাভাবে খুঁজে পাওয়া কঠিন ছিল — পুরো সাইট একটাই পাতা হিসেবে দেখা হতো, কোনো `meta description` বা `sitemap.xml`ও ছিল না।

**কী যোগ হলো:**
- `scripts/build_index.py`-এ `generate_topic_pages()` — প্রতিটা টপিকের জন্য `docs/topic/<slug>/index.html` বানায়: টাইটেল, `meta description`, `canonical` URL, Open Graph ট্যাগসহ প্রকৃত static HTML (গুগল crawl করে সরাসরি পড়তে পারে), যা লোড হওয়ার সাথে সাথে মূল অ্যাপে (`/#slug`) রিডাইরেক্ট করে — সাধারণ ভিজিটরের অভিজ্ঞতা অপরিবর্তিত, পুরনো `#slug` লিংকও আগের মতোই কাজ করে।
- `generate_sitemap_and_robots()` — `docs/sitemap.xml` (হোমপেজ + প্রতিটা টপিক পাতা) ও `docs/robots.txt` বানায়।
- `docs/index.html`-এ হোমপেজের জন্যও `meta description` ও `canonical` লিংক যোগ হলো।
- `.github/workflows/update-wiki.yml`-এ `docs/topic/`, `docs/sitemap.xml`, `docs/robots.txt` auto-commit তালিকায় যোগ হলো — ভবিষ্যতে টপিক যোগ/মুছা/rename হলে এই SEO পাতাগুলোও স্বয়ংক্রিয়ভাবে সিঙ্কে থাকবে।

**যাচাই পদ্ধতি:** ক্লিন রিবিল্ড চালিয়ে reproducibility নিশ্চিত করা হয়েছে; ইচ্ছাকৃতভাবে একটা টপিকের `title` মুছে দিয়ে নিশ্চিত করা হয়েছে বিল্ড সঠিকভাবে ব্যর্থ হয় ও কোনো আধা-তৈরি আউটপুট রেখে যায় না; sitemap.xml-এর XML গঠন যাচাই করা হয়েছে; জেনারেট হওয়া প্রতিটা স্লাগ-ফোল্ডারের নাম বিদ্যমান slug-validation নিয়মের সাথে মিলিয়ে দেখা হয়েছে; Cloudflare Pages-এর route-matching ও SPA-fallback ডকুমেন্টেশন যাচাই করে নিশ্চিত হওয়া হয়েছে যে বিদ্যমান static ফাইল থাকলে সেটাই অগ্রাধিকার পায়; `git add docs/topic/` মুছে ফেলা পাতাও ঠিকভাবে স্টেজ করে তা পরীক্ষা করে নিশ্চিত করা হয়েছে (temporary টেস্ট কমিট পরে বাতিল করা হয়েছে)।

**VERSION বাড়ানো হলো:** 1.4.0 → 1.5.0 (নতুন system-level ক্যাপাবিলিটি, breaking change না)।

---

**যা করা হয়েছে: পুরো প্রজেক্ট আবার গভীরভাবে যাচাই করে ধরা পড়া অসংগতি ঠিক করা হলো (ব্যবহারকারীর অনুরোধে)**

**ঠিক করা হয়েছে (ডকুমেন্টেশন):**
- `README.md` — `VERSION` রেফারেন্স (`1.2.0` লেখা ছিল, আসল VERSION ফাইলে `1.4.0`) ঠিক করা হলো। topic file সংখ্যা ২৭ থেকে ২৬-এ আপডেট করা হলো (নিচে বর্ণিত মার্জের কারণে)।
- `TEST_CHECKLIST.md` — ভাসমান সার্চ বাডি ও `[[topic-slug]]` লিংক-ব্যাজ ফিচার দুটোর জন্য চেক-আইটেম যোগ করা হলো; আগে এই দুইটা ফিচার README/CHANGELOG-এ বর্ণিত থাকলেও checklist-এ কোনো যাচাই-ধাপ ছিল না।

**কনটেন্ট-লেভেল ফিক্স (এই ফাইলের বাইরে, টপিক/ঘটনাপ্রবাহ ফাইলে):** `sangsad-troyodosh.md`/`sangsad-jatra.md` ডুপ্লিকেট টপিক মার্জ, এবং একটা ঘটনাপ্রবাহ এন্ট্রির প্রধানমন্ত্রীর নাম ঠিক করা — এই দুইটার বিস্তারিত এই ফাইলে না রেখে সংশ্লিষ্ট টপিক ফাইলের নিজের "পরিবর্তনের ইতিহাস"-এ রাখা হয়েছে (উপরের নিয়ম অনুযায়ী)।

VERSION বাড়ানো হয়নি — খাঁটি ডকুমেন্টেশন/কনটেন্ট সংশোধন, কোনো কোড বা ফিচার বদলায়নি।

---

## [1.0.0] — ২০২৬-০৭-১৮

**যা করা হয়েছে: প্রাথমিক ভার্সন হিসেবে রিসেট করা হলো (ব্যবহারকারীর অনুরোধে)**

আগের ইন্টারনাল ডেভেলপমেন্ট ভার্সনগুলোর (পুরনো নম্বরিং ও তাদের সাথে সম্পর্কিত ইতিহাস) রেকর্ড এখান থেকে মুছে ফেলা হয়েছে। এই প্রজেক্ট এখন থেকে **v1.0.0** কে ভিত্তি ধরে নতুন করে ভার্সনিং শুরু করবে, যাতে সামনে যোগ হওয়া নতুন ফিচারগুলো একটা পরিষ্কার, গোছানো ইতিহাস হিসেবে ট্র্যাক করা যায়।

**এই মুহূর্তে প্রজেক্টের অবস্থা (v1.0.0 বেসলাইন):**
- টপিক-ভিত্তিক knowledge-base (search, topic reading, recent changes, dashboard, আজকের রিভিশন, dark mode, offline support)
- কুইজ/MCQ ফিচার নেই (ইচ্ছাকৃতভাবে বাদ দেওয়া হয়েছে)
- GitHub Actions workflow (`.github/workflows/update-wiki.yml`) দিয়ে push-এর পর অটোমেটিক build
- `docs/version.json` ফুটারে ভার্সন নম্বর দেখানোর জন্য ব্যবহৃত হয়
- BOM (Byte Order Mark)-যুক্ত টপিক ফাইল (যেমন Windows Notepad দিয়ে সেভ করা) থাকলেও
  বিল্ড বা টপিক-পেজ রেন্ডারিং না ভেঙে নীরবে সঠিকভাবে পার্স হয় (`scripts/build_index.py`
  ও `docs/index.html` দুই জায়গাতেই BOM স্ট্রিপ করা হয়)
---

## [1.1.0] — ২০২৬-০৭-১৯

**যা করা হয়েছে: "দৈনিক ঘটনাপ্রবাহ" নামে নতুন ফিচার যোগ, আর ফাইল-কাঠামো সহজ করা হলো**

এই আপডেটে দুই ধরনের কাজ হয়েছে — একটা নতুন ফিচার তৈরি, আর একটা কাঠামোগত পরিষ্কার-পরিচ্ছন্নতা। নিচে সহজ ভাষায় প্রতিটা লেখা হলো, যাতে পরে পড়লে বোঝা যায় কী হয়েছিল আর কেন।

### ১. নতুন ফিচার: 🗓️ দৈনিক ঘটনাপ্রবাহ

**কী যোগ হলো:** সাইটের উপরের টুলবারে (ডার্ক মোড বাটনের পাশে) একটা নতুন বাটন — "🗓️ দৈনিক ঘটনাপ্রবাহ"। এখানে ক্লিক করলে প্রতিমাসের দিন-ভিত্তিক ঘটনার তালিকা দেখা যায় (বাংলাদেশ/আন্তর্জাতিক ইত্যাদি ক্যাটাগরি অনুযায়ী ভাগ করা)।

**কীভাবে কাজ করে:**
- এই ঘটনাগুলো `docs/ghotonaprobaho/` নামের একটা আলাদা ফোল্ডারে `.md` ফাইল আকারে রাখা হয় (ঠিক যেভাবে `docs/topics/`-এ টপিকগুলো রাখা হয়)।
- প্রতিটা ফাইলে তারিখ-ভিত্তিক এন্ট্রি লেখা থাকে (যেমন: `## ২৫ এপ্রিল ২০২৬`, তারপর ক্যাটাগরি ও বুলেট-পয়েন্ট)।
- একবারে সাম্প্রতিক **৩ মাসের তথ্য টানা একসাথে** দেখানো হয় (মাস ভাগ করে হেডিং দিয়ে), আর নিচে "← আগের ৩ মাস / পরের ৩ মাস →" বাটন দিয়ে পুরনো/নতুন মাসে যাওয়া যায়।
- **একটা গুরুত্বপূর্ণ বিষয় মাথায় রাখা হয়েছে:** কোনো একটা ফাইলে যদি এক মাসের শেষ কয়েক দিন আর পরের মাসের শুরু কয়েক দিন একসাথে লেখা থাকে (যেমন এপ্রিলের শেষ ৫ দিন + মে মাসের পুরোটা একই ফাইলে), তাহলেও প্রতিটা দিন তার নিজস্ব তারিখ অনুযায়ী সঠিক মাসের গ্রুপেই দেখানো হয় — ফাইলের নাম বা বিভাজনের উপর নির্ভর করে না। এটা টেস্ট করে নিশ্চিত করা হয়েছে।

### ২. ফাইল-কাঠামো সহজ করা: এক জায়গায় ফাইল রাখা

**সমস্যা যা ছিল:** আগে `topics/` ফোল্ডার (টপিক লেখার জন্য) আর `docs/topics/` ফোল্ডার (ওয়েবসাইটে দেখানোর জন্য) — এই দুই জায়গায় একই কনটেন্টের কপি থাকত। প্রতিবার সাইট আপডেট হওয়ার সময় প্রথমটা থেকে দ্বিতীয়টাতে স্বয়ংক্রিয়ভাবে কপি হতো। একই ব্যবস্থা `ghotonaprobaho/` আর `docs/ghotonaprobaho/`-এও ছিল।

**ব্যবহারকারীর অনুরোধে যা করা হলো:** এখন থেকে টপিক ও ঘটনাপ্রবাহ ফাইল **শুধু একটাই জায়গায়** থাকে —
- টপিক লেখা/এডিট করার জায়গা: `docs/topics/`
- ঘটনাপ্রবাহ লেখা/এডিট করার জায়গা: `docs/ghotonaprobaho/`

আলাদা root-level `topics/` বা `ghotonaprobaho/` ফোল্ডার আর নেই। এতে বিভ্রান্তি কমবে — এখন থেকে যেকোনো নতুন টপিক বা ঘটনাপ্রবাহ ফাইল সরাসরি `docs/` এর ভেতরের ফোল্ডারেই রাখতে হবে।

### ৩. অতিরিক্ত ছোট সংযোজন: ক্যাশ রিফ্রেশ বাটন

সাইটের ফুটারে (একদম নিচে) একটা "🧹 সর্বশেষ ভার্সন লোড করুন" বাটন যোগ করা হয়েছে। সাইটে অটোমেটিক পুরনো ভার্সন মুছে নতুন ভার্সন দেখানোর ব্যবস্থা আগে থেকেই আছে, কিন্তু কখনো কখনো ব্রাউজার পুরনো তথ্য আটকে রাখতে পারে — তখন এই বাটনে ক্লিক করলে সব পুরনো সংরক্ষিত তথ্য মুছে জোর করে সর্বশেষ ভার্সন লোড হবে।

**এই মুহূর্তে প্রজেক্টের অবস্থা (v1.1.0):**
- টপিক-ভিত্তিক knowledge-base (search, topic reading, recent changes, dark mode, offline support)
- নতুন: দৈনিক ঘটনাপ্রবাহ ফিচার (৩-মাস-ভিত্তিক পেজ আকারে দেখানো)
- নতুন: ম্যানুয়াল ক্যাশ-রিফ্রেশ বাটন
---

## [1.1.1] — ২০২৬-০৭-১৯

**যা করা হয়েছে: ফন্ট বদলানো হলো (স্পষ্টতা বাড়াতে)**

আগে সাইটে "Tiro Bangla" নামের ফন্ট ব্যবহার হতো, কিন্তু সেটা পড়তে স্পষ্ট মনে হচ্ছিল না। ব্যবহারকারীর দেওয়া **কালপুরুষ (Kalpurush)** ফন্ট দিয়ে পুরো সাইটের সব লেখা বদলে দেওয়া হয়েছে (হেডিং, বাটন, টপিক-কনটেন্ট, ট্যাগ — সব জায়গায়)।

- পুরনো ফন্ট ফাইল (`TiroBangla-Regular.ttf`, `TiroBangla-Italic.ttf`) সরিয়ে ফেলা হয়েছে
- নতুন ফাইল: `docs/fonts/Kalpurush.ttf`
- কালপুরুষে আলাদা italic ভ্যারিয়েন্ট নেই, তাই এক ফাইলই normal স্টাইলের জন্য ব্যবহার করা হয়েছে

---

## [1.1.4] — ২০২৬-০৭-২০

**যা করা হয়েছে: তিনটে ফন্টের সমন্বয় + কনট্রাস্ট ফিক্স + দুটো বাগ সারানো**

এই আপডেটে আগের রঙ/কনট্রাস্ট রিপোর্ট অনুযায়ী কাজ করা হয়েছে, তারপর সেই কাজেই পাওয়া দুটো বাগ ঠিক করা হয়েছে।

**ফন্ট সিস্টেম:**
- এখন তিনটে ফন্ট একসাথে ব্যবহার হয় — **Kalpurush** (UI/নেভিগেশন/বাটন), **Hind Siliguri** (হেডিং), **Noto Serif Bengali** (টপিকের মূল পড়ার টেক্সট)
- নতুন ফন্ট ফাইল: `HindSiliguri-SemiBold.woff2`, `HindSiliguri-Medium.woff2`, `NotoSerifBengali-Regular.woff2`, `NotoSerifBengali-Bold.woff2` (সবগুলো বাংলা+ল্যাটিন ইউনিকোড রেঞ্জে সাবসেট করে ছোট রাখা হয়েছে)

**কনট্রাস্ট/রঙ ফিক্স:**
- ডার্ক মোডে extreme contrast নরম করা হয়েছে (এখনও AAA মানের ওপরে, তবে চোখে কম চাপ)
- টেবিল-হেডার ও active ট্যাবের সবুজ ডার্ক মোডে কম উজ্জ্বল করা হয়েছে
- হার্ডকোডেড `#fff` টোকেন-ভিত্তিক করা হয়েছে; সিস্টেম dark-mode preference অনুযায়ী প্রথমবার থিম ঠিক হয়

**বাগ ফিক্স (এই আপডেটেই ধরা পড়েছে ও সারানো হয়েছে):**
- **`history-heading` ফন্ট বাগ:** "পরিবর্তনের ইতিহাস" হেডিং Kalpurush-এ রাখার ইচ্ছা থাকলেও, CSS specificity-র কারণে (`.card-body h2` বনাম `.history-heading`) সেটা ভুলবশত Hind Siliguri-তে দেখাচ্ছিল। সমাধান: selector-কে `.card-body h2.history-heading` করে `!important` যোগ করা হয়েছে, যাতে ভবিষ্যতেও অন্য কোনো নতুন নিয়ম এসে এটা আবার ভেঙে না দেয়।
- **Active tab-tags হার্ডকোডেড রঙ:** `.tab.active .tab-tags`-এ `rgba(255,255,255,0.75)` (সরাসরি সাদা) ব্যবহার হচ্ছিল, যেখানে বাকি সব "ডার্ক ব্যাকগ্রাউন্ডের ওপর টেক্সট" জায়গায় `var(--on-dark)` টোকেন ব্যবহার হয়। আগের রাউন্ডে "৫টা hardcoded #fff ঠিক করা" সার্চে এটা ধরা পড়েনি কারণ এটা আক্ষরিক `#fff` না, `rgba()` ফর্মে লেখা ছিল। এখন `var(--on-dark)` + `opacity:0.75` দিয়ে থিম-সামঞ্জস্যপূর্ণ করা হয়েছে।

**শেখার বিষয় (ভবিষ্যতের জন্য নোট):** হার্ডকোডেড রঙ খোঁজার সময় শুধু `#fff`/`#000` না, `rgba(255,255,255,...)` ও `rgba(0,0,0,...)` ফর্মেও সার্চ করা দরকার।

---

## [1.2.0] — ২০২৬-০৭-২০

**যা করা হয়েছে: প্রজেক্টের নাম "JobArchive" থেকে "Open Current Affairs"-এ বদলানো হলো (ব্যবহারকারীর অনুরোধে)**

পুরো কোডবেসে যেখানে যেখানে পুরনো নাম ব্যবহৃত হচ্ছিল, সেসব জায়গা খুঁজে বদলানো হয়েছে:

- `docs/index.html` — `<title>` ও `localStorage` key দুটো (`jobarchive_progress` → `oca_progress`, `jobarchive_theme` → `oca_theme`)
- `docs/manifest.json` — `name` ও `short_name`
- `scripts/sw_template.js` ও `docs/sw.js` (generated) — কমেন্ট ও `CACHE_NAME` (`jobarchive-cache-*` → `oca-cache-*`)
- `scripts/build_index.py` — বিল্ড লগ মেসেজে cache নাম রেফারেন্স
- `wrangler.toml` — Cloudflare Pages project `name` (`jobarchive` → `open-current-affairs`)
- `README.md`, `PROJECT.md` — প্রজেক্ট শিরোনাম ও ফোল্ডার-কাঠামো ডায়াগ্রামের লেবেল

**গুরুত্বপূর্ণ (breaking change):** `localStorage` key বদলানোর সিদ্ধান্ত ব্যবহারকারীর সরাসরি অনুমোদনে নেওয়া হয়েছে — এর ফলে যারা আগে সাইট ভিজিট করেছেন, তাদের সেভ করা reading progress ও dark/light mode পছন্দ নতুন ভার্সনে রিসেট হয়ে যাবে (নতুন key খালি অবস্থা থেকে শুরু হবে)। এটা সচেতনভাবে করা হয়েছে, পুরনো key-র সাথে সামঞ্জস্য রাখার (migration) বদলে পুরোপুরি consistent নতুন নামকরণের জন্য।

ওয়েবসাইটের দৃশ্যমান UI heading (`<h1>কারেন্ট অ্যাফেয়ার্স রেজিস্টার</h1>`)-এ আগে থেকেই "JobArchive" শব্দটা ছিল না, তাই সেটা অপরিবর্তিত রাখা হয়েছে।

`python3 scripts/build_index.py` চালিয়ে generated output (`docs/sw.js`, `docs/version.json`, `docs/topics-index.json`, `docs/recent-changes.json`, `docs/ghotonaprobaho-index.json`) নতুন ভার্সন নম্বর ও cache নাম দিয়ে রিফ্রেশ করা হয়েছে।

---

## [Unreleased] — ডকুমেন্টেশন ফিক্স, ২০২৬-০৭-২০

**যা করা হয়েছে: `README.md`, `PROJECT.md`, `TEST_CHECKLIST.md`-কে আসল single-copy `docs/topics/` architecture-এর সাথে মিলিয়ে ঠিক করা হলো**

রিব্র্যান্ডিংয়ের পর পুরো প্রজেক্ট বাগের জন্য পুনরায় পরীক্ষা করার সময় ধরা পড়ে যে `scripts/build_index.py`-এর কোড (এবং তার নিজের comment) অনুযায়ী root-level আলাদা `topics/` সোর্স ফোল্ডার আর নেই — `TOPICS_DIR` সরাসরি `docs/topics/` নির্দেশ করে এবং কোনো `copytree`/কপি ধাপ নেই (`docs/topics/`-ই একইসাথে সোর্স এবং deploy হওয়া কনটেন্ট)। কিন্তু এই তিনটা ডকুমেন্টেশন ফাইল তখনও পুরনো দুই-ফোল্ডার ব্যবস্থা (`topics/` → `docs/topics/` কপি) বর্ণনা করছিল। এটা কোনো নতুন কোড-বাগ ছিল না (আপলোড করা zip-এই root-level `topics/` অনুপস্থিত ছিল), কিন্তু ডকুমেন্টেশন-কোড mismatch হিসেবে ভুল নির্দেশনা তৈরি করছিল — বিশেষভাবে `TEST_CHECKLIST.md`-এর একটা আইটেম আক্ষরিক অর্থেই সম্পন্ন করা অসম্ভব ছিল, কারণ যেই `topics/` ফোল্ডারের সাথে তুলনা করতে বলা হচ্ছিল সেটাই নেই।

**ঠিক করা হয়েছে:**
- `PROJECT.md` — ফোল্ডার-ডায়াগ্রাম, source-of-truth ব্যাখ্যা, build flow (কপি-ধাপ বাদ), roadmap section, আর একটা historical নোট যোগ করা হলো যাতে এই architecture-পরিবর্তন ভবিষ্যতে ট্র্যাক করা যায়
- `README.md` — সব `topics/` রেফারেন্স `docs/topics/`-এ বদলানো হলো (৫ জায়গায়)
- `TEST_CHECKLIST.md` — যেই চেকলিস্ট-আইটেম করাই অসম্ভব ছিল সেটা অর্থবহ একটা চেকে বদলানো হলো (`docs/topics/`-এর প্রতিটা valid ফাইল ইনডেক্স হয়েছে কিনা, এবং topic count মিলছে কিনা)

কোনো কোড ফাইল বদলায়নি — এটা খাঁটি ডকুমেন্টেশন সংশোধন, তাই VERSION বাড়ানো হয়নি।

---

## [Unreleased] — গভীর অডিটে ধরা পড়া অসংগতি ঠিক করা হলো, ২০২৬-০৭-২৬

**যা করা হয়েছে: ডকুমেন্টেশন, অফলাইন-ক্যাশ ও পুরনো ডিজাইন-ফাইল সংক্রান্ত চারটা সমস্যা ঠিক করা হলো (ব্যবহারকারীর অনুরোধে, পুরো প্রজেক্ট গভীরভাবে পরীক্ষা করার পর)**

**ঠিক করা হয়েছে:**
- `PROJECT.md` (লাইন ৫) — website-এর ফিচার তালিকা থেকে অস্তিত্বহীন "dashboard, daily revision strip" বাদ দিয়ে আসল ফিচার (দৈনিক ঘটনাপ্রবাহ) দিয়ে ঠিক করা হলো। এটা লাইন ৬৪-এর সাথে (যেখানে বলা আছে quiz/dashboard/revision scope-এর বাইরে) স্ববিরোধী ছিল।
- `TEST_CHECKLIST.md` — "আজকের রিভিশন" ও "Dashboard" যাচাই করার আইটেম বাদ দিয়ে বাস্তবে-থাকা ফিচারের (সাম্প্রতিক পরিবর্তন, দৈনিক ঘটনাপ্রবাহ) চেক দিয়ে বদলানো হলো, এবং স্পষ্ট করে লেখা হলো যে dashboard/quiz স্ক্রিন না-থাকাটাই কাঙ্ক্ষিত অবস্থা।
- `scripts/sw_template.js` — `APP_SHELL` (install-টাইম ক্যাশ তালিকা)-এ `ghotonaprobaho-index.json` এবং ৫টা ফন্ট ফাইল (`docs/fonts/*`) যোগ করা হলো। আগে এই ফাইলগুলো তালিকায় না থাকায়, কেউ যদি সাইট প্রথমবারই অফলাইনে খুলত (একবারও অনলাইনে "দৈনিক ঘটনাপ্রবাহ" না দেখে থাকলে), তাহলে সেই ফিচার কাজ করত না এবং বাংলা ফন্টও সিস্টেম ফন্টে ফলব্যাক করত। `docs/sw.js` টেমপ্লেট থেকে রিবিল্ড করা হয়েছে।
- `design-drafts/design-preview.html` (৮৭৭ KB) মুছে ফেলা হলো — এতে embed করা পুরনো "Tiro Bangla" ফন্ট v1.1.1-এই লাইভ সাইট থেকে সরানো হয়েছিল এবং Kalpurush/Hind Siliguri/Noto Serif Bengali দিয়ে বদলানো হয়েছিল। কোথাও এই ফাইলের রেফারেন্স ছিল না (build/deploy pipeline-এর অংশ ছিল না), তাই এটা একটা "ভুলে যাওয়া" পুরনো, বাস্তবতার সাথে অসামঞ্জস্যপূর্ণ ফাইল ছিল। খালি হয়ে যাওয়া `design-drafts/` ফোল্ডারও সরানো হলো।
- (re-verification-এ ধরা পড়া, উপরের চারটা ফিক্সের সাথেই একইসাথে ঠিক করা হলো) `README.md`-এর "এই রিপোতে কী কী আছে" তালিকা থেকে এখন-মুছে-ফেলা `design-drafts/`-এর লাইন বাদ দেওয়া হলো, এবং একই ফাইলের একটা ⚠️ সতর্কতা-নোট (যেটা আগে `PROJECT.md`-র dashboard/revision অসংগতি নিয়ে সতর্ক করত) বাদ দেওয়া হলো যেহেতু সেই মূল সমস্যা উপরেই ঠিক হয়ে গেছে — নোটটা রেখে দিলে এখন নিজেই একটা stale/ভুল তথ্য হয়ে থাকত।

**যাচাই পদ্ধতি:** ফিক্সের পর `python3 scripts/build_index.py` আবার চালিয়ে ২৭/২৭ ভ্যালিডেশন পাস নিশ্চিত করা হয়েছে; rebuild-এর আগে/পরে `docs/`-এর diff নিয়ে কনফার্ম করা হয়েছে জেনারেটেড আউটপুট সোর্সের সাথে sync-এ আছে; `APP_SHELL`-এর প্রতিটা path প্রোগ্রাম্যাটিকভাবে ফাইল-সিস্টেমের বিপরীতে চেক করা হয়েছে; `index.html`-এর প্রতিটা `getElementById` কল স্ট্যাটিক HTML-এর `id` attribute-এর সাথে ক্রস-চেক করা হয়েছে; এবং আসল `docs/vendor/marked.min.js` + `purify.min.js` দিয়ে সবগুলো ২৭টা topic body সত্যিকারভাবে রেন্ডার করে দেখা হয়েছে (Node + jsdom দিয়ে) — কোনো error, খালি আউটপুট, বা sanitizer-এর অস্বাভাবিক content-strip পাওয়া যায়নি।

**সংশোধন করা হয়নি (কনটেন্ট প্রশ্ন, কোড-সমস্যা নয়):** `docs/topics/podok-purushkar.md`-এর "৯৮তম অস্কার" ও "আবেল পুরস্কার ১৮৯৯" সংক্রান্ত অংশে যে বিবরণ বাস্তবতার সাথে না মেলার সম্ভাবনা আছে বলে আগের রিপোর্টে জানানো হয়েছিল, তা এই দফায় বদলানো হয়নি — কারণ এই টেক্সট আসল মাসিক সোর্স (`archive/2026-04.md`)-এ হুবহু একইভাবে আছে, এবং সত্যতা যাচাই করে কনটেন্ট বদলানোর সিদ্ধান্ত ব্যবহারকারীর নেওয়া উচিত।

VERSION বাড়ানো হয়নি — এটা মূলত ডকুমেন্টেশন সংশোধন ও একটা ছোট, non-breaking ক্যাশ-উন্নতি (আগের পুরনো ভার্সন ব্যবহারকারীরা নতুন cache নাম না পাওয়া পর্যন্ত পুরনো আচরণই পাবে, কোনো ভাঙন নেই)।

---

## [Unreleased] — দ্বিতীয় দফার গভীর অডিটে ধরা পড়া কোড-বাগ ও নির্ভরতা (dependency) আপডেট, ২০২৬-০৭-২৬

**যা করা হয়েছে: ব্যবহারকারীর অনুরোধে আরও গভীরভাবে (CSS cascade, print behavior, third-party library version) পরীক্ষা করে ৩টা প্রকৃত bug ও একটা নিরাপত্তা-সংক্রান্ত dependency আপডেট ঠিক করা হলো**

**ঠিক করা হয়েছে (কোড):**
- `docs/index.html` — Print stylesheet (`@media print`)-এ `#ghotonaprobaho-layout` ও `.above-drawer-row` হাইড করার নিয়ম যোগ করা হলো। আগে এই দুইটা `@media print` ব্লকে না থাকায়, কেউ "দৈনিক ঘটনাপ্রবাহ" মোডে থাকা অবস্থায় প্রিন্ট করলে (Ctrl+P) খালি "browse" placeholder-এর সাথে ghotonaprobaho-এর কনটেন্টও একসাথে ওভারল্যাপ করে প্রিন্ট হয়ে যেত। একটা real CSS parser (css-tree) দিয়ে cascade resolve করে যাচাই করা হয়েছে যে ফিক্সের পর এই সমস্যা নেই।
- `docs/index.html` — মোবাইল CSS-এ `.card h2` ও `.card .tags-row`-এর `margin-left:66px` (৭৬০px breakpoint-এ, সিল ব্যাজের জন্য জায়গা রাখতে) ৭০০px breakpoint-এর একটা পরের রুল দিয়ে `margin-left:0`-এ ওভাররাইড হয়ে যাচ্ছিল, কারণ ৭০০px-এর নিচে দুইটা media query-ই একসাথে সক্রিয় থাকে এবং সোর্স-অর্ডারে পরেরটাই জেতে। ফলে ৭০০px-এর নিচের স্ক্রিনে হেডিং টেক্সট ও ট্যাগগুলো সিল ব্যাজের সাথে ওভারল্যাপ করত। ৭০০px ব্লক থেকে conflicting `margin-left:0` বাদ দেওয়া হলো (শুধু `padding-right:0` রাখা হলো, যেটার সাথে কোনো conflict নেই)। css-tree দিয়ে ৫০০px viewport সিমুলেট করে যাচাই করা হয়েছে যে এখন সঠিকভাবে `66px` resolve হয়।

**ঠিক করা হয়েছে (নিরাপত্তা/dependency):**
- `docs/vendor/purify.min.js` — DOMPurify 3.4.11 থেকে 3.4.12-এ আপডেট করা হলো। 3.4.11-এ একটা প্রকাশিত advisory আছে (GHSA-c2j3-45gr-mqc4: `CUSTOM_ELEMENT_HANDLING` ব্যবহার করলে `afterSanitizeElements` হুক বাইপাস হতে পারে)। যাচাই করে দেখা হয়েছে এই সাইট `CUSTOM_ELEMENT_HANDLING` ব্যবহারই করে না (শুধু plain `DOMPurify.sanitize(html)` কল করে), তাই এই নির্দিষ্ট সাইট বাস্তবে exploitable ছিল না — তবু general security hygiene হিসেবে আপডেট করা হলো। ফাইলটা npm-এর অফিসিয়াল রেজিস্ট্রি থেকে (`npm install dompurify@3.4.12`) নেওয়া হয়েছে, কোনো আনভেরিফায়েড সোর্স থেকে না। আপডেটের পর সবগুলো ২৭টা টপিক আসল ফাইল দিয়ে আবার রেন্ডার করে drop-in compatibility নিশ্চিত করা হয়েছে।
- `docs/vendor/marked.min.js` (v18.0.5) পরীক্ষা করে দেখা হয়েছে — এটাই বর্তমানে "latest non-vulnerable version" হিসেবে চিহ্নিত (Snyk অনুযায়ী), তাই এটা বদলানো হয়নি।

**পর্যবেক্ষণ করা হয়েছে কিন্তু বদলানো হয়নি (ambiguous, judgment-call প্রকৃতির):**
- `:root`-এ ৪টা CSS ভ্যারিয়েবল (`--accent-deep`, `--error-bg`, `--error-text`, `--seal-text`) সংজ্ঞায়িত আছে কিন্তু stylesheet-এর কোথাও `var(--x)` দিয়ে ব্যবহার হয় না। সাইটে একাধিক real error/empty state আছে (`empty-note`, `card-placeholder`) কিন্তু সবগুলো generic `--muted` ধূসর রঙে দেখানো হয় — dedicated error-red styling বানানো হলেও কোথাও লাগানো হয়নি। এটা bug না, একটা visual-polish সুযোগ — সিদ্ধান্ত owner-এর নেওয়া উচিত মনে করে বদলানো হয়নি।
- `manifest.json`-এর `background_color` (`#eeebe1`) আর লাইভ CSS-এর `--paper` (`#f2f2ef`) হুবহু এক না (কাছাকাছি রঙ) — PWA কোল্ড-স্টার্টে সংক্ষিপ্ত সময়ের জন্য সামান্য রঙ-পার্থক্য দেখা যেতে পারে। এতটাই সামান্য যে এটা bug না বলে cosmetic নোট হিসেবে রাখা হলো।

**যাচাই পদ্ধতি:** প্রতিটা CSS ফিক্স `css-tree` (একটা real, standards-based CSS parser) দিয়ে প্রোগ্রাম্যাটিকভাবে cascade resolve করে যাচাই করা হয়েছে, শুধু ম্যানুয়াল রিডিং না। DOMPurify আপডেটের পর আসল `marked.min.js` + নতুন `purify.min.js` দিয়ে সবগুলো ২৭টা টপিক আবার রেন্ডার করে কোনো regression নেই তা নিশ্চিত করা হয়েছে। প্রতিটা ফিক্সের পর `python3 scripts/build_index.py` আবার চালিয়ে ২৭/২৭ ভ্যালিডেশন পাস ও CSS brace-balance নিশ্চিত করা হয়েছে।

VERSION বাড়ানো হয়নি — এগুলো bug-fix ও security-hygiene আপডেট, কোনো নতুন ফিচার বা breaking change না। তবে dependency ফাইল (`purify.min.js`) সত্যিই বদলেছে, তাই deploy করার আগে ব্রাউজারে একবার ম্যানুয়ালি smoke-test করে নেওয়া ভালো।

---

## [Unreleased] — তৃতীয় দফার যাচাই: pixel-level প্রমাণ ও আরেকটা ছোট ফিক্স, ২০২৬-০৭-২৬

**যা করা হয়েছে: আগের দুই দফার ফিক্স আসলেই সঠিক কিনা তা পিক্সেল-লেভেলে গাণিতিক হিসাব করে প্রমাণ করা হলো, এবং একটা নতুন ছোট (কিন্তু নিশ্চিতভাবে প্রকৃত) অসামঞ্জস্য ধরে ঠিক করা হলো**

**ঠিক করা হয়েছে:**
- `docs/index.html`-এর static `<meta name="theme-color">`-এর মান `#0e2620` থেকে `#004d2c`-এ বদলানো হলো, যাতে এটা `THEME_COLORS['light']`-এর মানের সাথে হুবহু মিলে যায়। কারণ: থিম-নির্ধারণ করা `initTheme()` ফাংশনটা `<body>`-এর একদম শেষে থাকা `<script>`-এ চলে (blocking, deferred না), তাই পেজ লোডের শুরুতে ব্রাউজার প্রথমে পুরনো/ভিন্ন মান (`#0e2620`) দিয়ে mobile browser chrome রং করত, তারপর `initTheme()` চলার পর নতুন মান (`#004d2c`)-এ বদলে যেত — একটা সংক্ষিপ্ত, প্রতি পেজ-লোডে ঘটা রং-ফ্ল্যাশ। এখন দুইটা মান শুরু থেকেই এক, তাই ফ্ল্যাশ নেই।

**যাচাই করা হয়েছে, pixel-level গাণিতিক প্রমাণ সহ:**
- আগের দফায় ঠিক করা মোবাইল মার্জিন বাগ — ডেস্কটপে সিল ব্যাজ ও হেডিং-এর মধ্যে ফাঁক ছিল ঠিক ২px (৯০px margin − ৮৮px সিল-এর ডান প্রান্ত)। ফিক্সের পর মোবাইলেও (৭০০px-এর নিচে) ফাঁক ঠিক ২px (৬৬px margin − ৬৪px সিল-এর ডান প্রান্ত) — এই মিল প্রমাণ করে যে ৬৬px-ই আসল, উদ্দিষ্ট মান ছিল, এবং ফিক্সটা সঠিক। ফিক্সের আগে এই মার্জিন ০ হয়ে যেত, যার ফলে হেডিং টেক্সট সরাসরি সিল ব্যাজের নিচে (৬৪px ওভারল্যাপ) ঢুকে যেত — এটাও গাণিতিকভাবে হিসাব করে দেখানো হয়েছে।
- Print CSS ফিক্স — `.card-actions` ও `.seal`-এর মতো অন্যান্য print-hide entry ঠিকভাবে অক্ষত আছে কিনা পুনরায় নিশ্চিত করা হয়েছে; কোনো JS-ভিত্তিক print-event handling (যেমন `beforeprint`) নেই বলে নিশ্চিত হওয়া গেছে যে এই ফিক্স শুধু CSS দিয়েই সম্পূর্ণ।
- থিম-সংক্রান্ত localStorage key (`oca_theme`)-এর প্রতিটা read/write location ক্রস-চেক করে দেখা হয়েছে — head-এর flash-প্রতিরোধ script আর মূল theme-toggle logic-এর মধ্যে কোনো ভ্যালু-অসামঞ্জস্য নেই।

**নতুন content flag (কোড-সমস্যা নয়, সোর্স কনটেন্টের সম্ভাব্য ত্রুটি):**
- `docs/ghotonaprobaho/2026-04-25_2026-05-18.md`-এ ২৭ এপ্রিলের এন্ট্রিতে "প্রধানমন্ত্রী শেখ হাসিনা তারেক রহমান" লেখা আছে — দুইটা ভিন্ন নাম একসাথে জোড়া লাগানো বলে মনে হচ্ছে। এটা `podok-purushkar.md`-এর আগে-পাওয়া অস্কার/আবেল-পুরস্কার সমস্যার মতোই একই ধরনের সম্ভাব্য কনটেন্ট-ত্রুটি — তবে এটা কোনো archive ফাইলের সাথে cross-verify করা যায়নি (এই নির্দিষ্ট ghotonaprobaho ফাইলের কোনো মিলে-যাওয়া archive সোর্স রিপোতে নেই)। বদলানো হয়নি, কারণ সঠিক নাম কী হওয়া উচিত তা অনুমান করে বসিয়ে দেওয়া ভুল হবে — owner-এর নিজে যাচাই করে ঠিক করা উচিত।

**সীমাবদ্ধতা:** এই পরিবেশে (sandboxed container) কোনো headless browser ইনস্টল করা সম্ভব হয়নি (নেটওয়ার্ক শুধু নির্দিষ্ট package registry-তে সীমাবদ্ধ), তাই প্রকৃত ব্রাউজারে রেন্ডার করে pixel-perfect স্ক্রিনশট নিয়ে যাচাই করা যায়নি। এর বদলে একটা real, standards-based CSS parser (`css-tree`) দিয়ে cascade গাণিতিকভাবে resolve করে, এবং CSS-এ ঘোষিত exact pixel মান দিয়ে জ্যামিতিক হিসাব করে যাচাই করা হয়েছে। প্রকৃত ব্রাউজারে একবার ম্যানুয়ালি (বিশেষ করে মোবাইল ভিউ ও প্রিন্ট প্রিভিউ) চেক করে নেওয়া এখনও ভালো অভ্যাস হবে।

VERSION বাড়ানো হয়নি — এটাও bug-fix, breaking change না।

---

## [1.3.0] — ২০২৬-০৭-২৭

**যা করা হয়েছে: "🕘 সাম্প্রতিক পরিবর্তন" নামে নতুন ফিচার যোগ হলো — build পাইপলাইনে আগে থেকেই তৈরি হওয়া `recent-changes.json` ডেটা এতদিন কোথাও দেখানো হতো না, এখন সেটার জন্য একটা পূর্ণাঙ্গ view বানানো হলো**

**সমস্যা যা ছিল:** প্রতিবার বিল্ড চালালে `docs/recent-changes.json` তৈরি হতো (প্রতিটা টপিকের "পরিবর্তনের ইতিহাস" টেবিল থেকে একত্র করে, উইকিপিডিয়ার Recent Changes-এর মতো), সার্ভিস ওয়ার্কার এটাকে অফলাইনের জন্য ক্যাশও করত — কিন্তু ওয়েবসাইটের কোডে এই ফাইলটা কোথাও fetch করাই হতো না। মানে ডেটা তৈরি হচ্ছিল, কিন্তু ব্যবহারকারী কখনো দেখতেই পেতেন না।

**কী যোগ হলো:** "🗓️ দৈনিক ঘটনাপ্রবাহ" বাটনের পাশে একটা নতুন বাটন — "🕘 সাম্প্রতিক পরিবর্তন"। এখানে ক্লিক করলে কোন টপিক কবে/কী কারণে আপডেট হলো তার একটা তালিকা দেখা যায়, মাস অনুযায়ী গ্রুপ করা।

**কীভাবে কাজ করে:**
- `recent-changes.json`-এর ডেটা মাস অনুযায়ী client-side-এ গ্রুপ করা হয় (ফাইলটা আগে থেকেই নতুন→পুরনো সাজানো থাকে, তাই গ্রুপ করার পরও ক্রম ঠিক থাকে)।
- ঘটনাপ্রবাহের মতোই একবারে ৩ মাসের এন্ট্রি একসাথে দেখানো হয়, "← আগের মাস / পরের মাস →" বাটন দিয়ে নেভিগেট করা যায়।
- প্রতিটা এন্ট্রিতে টপিকের নাম একটা ক্লিকযোগ্য লিংক হিসেবে থাকে — ক্লিক করলে সরাসরি সেই টপিক খুলে যায় (এবং "সাম্প্রতিক পরিবর্তন" মোড থেকে বেরিয়ে স্বাভাবিক browse মোডে ফিরে আসে)।
- তিনটা view (browse, দৈনিক ঘটনাপ্রবাহ, সাম্প্রতিক পরিবর্তন) এখন পরস্পর exclusive — যেকোনো একটাতে ক্লিক করলে বাকি দুইটা বন্ধ হয়ে যায় এবং তাদের টগল-বাটনও সঠিকভাবে "নিষ্ক্রিয়" অবস্থায় ফিরে আসে।

**যাচাই পদ্ধতি:** এবার আগের চেয়ে বেশি রিগোরাস টেস্টিং করা হয়েছে — শুধু কোড পড়ে না, বরং jsdom দিয়ে আসল পেজ (আসল marked.min.js, purify.min.js, আসল JSON ডেটা, আসল টপিক মার্কডাউন ফাইল সহ) লোড করে, বাটনে সত্যিকারের ক্লিক সিমুলেট করে, প্রতিটা ধাপে DOM-এর প্রকৃত অবস্থা (কোন layout দেখা যাচ্ছে, কোন বাটন active, hash কী আছে, card-এ কী রেন্ডার হলো) পরীক্ষা করে ৩৫টা automated check — সবগুলো pass করেছে। বিশেষভাবে যাচাই করা হয়েছে যে ঘটনাপ্রবাহ মোডে থাকা অবস্থায় "সাম্প্রতিক পরিবর্তন"-এ ক্লিক করলে আগের মোড ঠিকভাবে বন্ধ হয়ে যায় (স্টাক/আটকে-থাকা বাটন-স্টেট বাগ নেই)।

**VERSION বাড়ানো হলো:** 1.2.0 → 1.3.0 (নতুন user-facing ফিচার, breaking change না) — ব্যবহারকারীর অনুমতি নিয়ে, প্রজেক্টের নিজস্ব নিয়ম অনুযায়ী।

**এই মুহূর্তে প্রজেক্টের অবস্থা (v1.3.0):**
- টপিক-ভিত্তিক knowledge-base (search, topic reading, dark mode, offline support)
- দৈনিক ঘটনাপ্রবাহ ফিচার (৩-মাস-ভিত্তিক পেজ আকারে)
- নতুন: সাম্প্রতিক পরিবর্তন ফিচার (মাস-ভিত্তিক পেজ আকারে, টপিক-লিংক সহ)

---

## [1.4.0] — ২০২৬-০৭-২৭

**যা করা হয়েছে: দৈনিক ঘটনাপ্রবাহ থেকে বিদ্যমান টপিকে ক্লিকযোগ্য লিংক করার সুবিধা যোগ হলো**

**সমস্যা যা ছিল:** কিছু টপিক (GDP পরিসংখ্যান, বৈশ্বিক সূচক, রিপোর্ট-সমীক্ষা) দ্রুত ও নিয়মিত আপডেট হয়। যখন দৈনিক ঘটনাপ্রবাহে এমন একটা আপডেট লেখা হতো, তখন সেটা বিদ্যমান টপিকের আপডেট নাকি সম্পূর্ণ নতুন কিছু — এই পার্থক্য কোথাও স্পষ্ট থাকত না, যা বিভ্রান্তি তৈরি করতে পারত (আগে সংসদ নিয়ে দুইটা আলাদা, পরস্পরবিরোধী টপিক তৈরি হয়ে যাওয়ার মতো সমস্যা)।

**কী যোগ হলো:** `docs/ghotonaprobaho/*.md`-এ যেকোনো বুলেট-লাইনের শেষে `[[topic-slug]]` লিখলে সেই এন্ট্রি সংশ্লিষ্ট টপিকের সাথে link হয়ে যায়। ওয়েবসাইটে সেই এন্ট্রির পাশে একটা ছোট, ক্লিকযোগ্য ব্যাজ (🔗 টপিকের নাম) দেখা যায় — ক্লিক করলে সরাসরি সেই টপিকের সর্বশেষ পূর্ণাঙ্গ তথ্যে চলে যাওয়া যায়।

**কীভাবে কাজ করে:**
- `scripts/build_index.py`-এর ঘটনাপ্রবাহ-পার্সার এখন প্রতিটা বুলেট আইটেমে `[[slug]]` প্যাটার্ন খোঁজে। পেলে সেটা মূল টেক্সট থেকে আলাদা করে `related_topic` ফিল্ড হিসেবে সংরক্ষণ করে।
- **কড়া ভ্যালিডেশন:** `[[slug]]`-এর slug অবশ্যই `docs/topics/`-এ বিদ্যমান কোনো টপিকের সাথে মিলতে হবে, নাহলে পুরো build ব্যর্থ হয়ে যাবে, ফাইলের নাম ও ভুল slug-সহ স্পষ্ট error message দেখাবে। এটা টাইপো বা রিনেম-হওয়া/মুছে-ফেলা টপিকের রেফারেন্স ধরার জন্য ইচ্ছাকৃতভাবে কড়া রাখা হয়েছে।
- ওয়েবসাইটের দৈনিক ঘটনাপ্রবাহ view-তে, `related_topic` থাকা এন্ট্রিগুলোতে টপিকের আসল শিরোনাম দেখিয়ে একটা লিংক-ব্যাজ বসে। ক্লিক করলে ঘটনাপ্রবাহ মোড থেকে বেরিয়ে সরাসরি সেই টপিক খুলে যায় (আগে থেকে থাকা mode-switching guard লজিক পুনর্ব্যবহার করে, নতুন কোনো bug-ঝুঁকি ছাড়াই)।
- বর্তমান `docs/ghotonaprobaho/2026-04-25_2026-05-18.md` ফাইলে **কোনো `[[slug]]` যোগ করা হয়নি** — কারণ যাচাই করে দেখা গেছে এই ফাইলের কভার করা সময়সীমার (এপ্রিল-মে ২০২৬) সাথে বিদ্যমান রিপোর্ট/সমীক্ষা-টপিকগুলোর তথ্যের (মার্চ ২০২৬-এ প্রকাশিত) কোনো প্রকৃত মিল নেই। fabricated/মিথ্যা লিংক যোগ না করে ফিচারটা শুধু প্রস্তুত রাখা হয়েছে — পরবর্তী কনটেন্ট-আপডেট সেশনে সত্যিকারের মিল পাওয়া গেলে ব্যবহার করা যাবে।
- `EDITORIAL_MEMORY.md`-এ এই নিয়ম স্থায়ীভাবে লিখে রাখা হয়েছে (সেকশন ২-এর প্রথম এন্ট্রি), যাতে ভবিষ্যতের কনটেন্ট-আপডেট সেশনে Claude নিজে থেকেই এই প্যাটার্ন প্রয়োগ করতে পারে।

**যাচাই পদ্ধতি:** jsdom দিয়ে আসল পেজ লোড করে, একটা synthetic (কিন্তু বাস্তব `gdp-bangladesh` টপিক ব্যবহার করা) ঘটনাপ্রবাহ ডেটাসেট দিয়ে যাচাই করা হয়েছে — লিংক-ছাড়া এন্ট্রি ঠিকভাবে প্লেইন টেক্সট দেখায়, লিংক-সহ এন্ট্রি সঠিক টপিকের শিরোনাম দেখায়, এবং ক্লিক করলে সত্যিই সঠিক টপিক খুলে যায় (mode, layout visibility, URL hash, ও রেন্ডার-হওয়া কনটেন্ট — সবগুলো cross-check করে)। এছাড়া Python-এ সরাসরি একটা ভুল slug দিয়ে টেস্ট করে নিশ্চিত হওয়া হয়েছে যে build সত্যিই ব্যর্থ হয় স্পষ্ট error-সহ। মোট ১৪টা automated check — সবগুলো pass করেছে। বিদ্যমান (কোনো লিংক-ছাড়া) ঘটনাপ্রবাহ ডেটা দিয়েও পুরো build backward-compatible ভাবে চলে তা নিশ্চিত করা হয়েছে।

**VERSION বাড়ানো হলো:** 1.3.0 → 1.4.0 (নতুন user-facing ফিচার, breaking change না)।
